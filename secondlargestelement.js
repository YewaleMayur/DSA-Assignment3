const arr = [12, 35, 1, 10, 1];
arr.sort((a, b) => b - a); // Sort the array in descending order
const secondLargest = arr[1]; // Access the second element
console.log(secondLargest); // Output the second largest element
